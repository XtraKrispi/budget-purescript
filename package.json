{
  "name": "budget-ui",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "postinstall": "psc-package install",
    "build": "pulp build --to dist/app.js && parcel build assets/index.html",
    "build-dev": "pulp build",
    "watch-dev": "pulp -w build",
    "build-serve": "pulp build --to dist/app.js && parcel build assets/index.html && http-server dist",
    "watch-serve": "pulp -w --then \"parcel build assets/index.html && http-server dist\" build --to dist/app.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "parcel-bundler": "^1.11.0",
    "psc-package": "^3.0.1",
    "pulp": "^12.3.1",
    "purescript": "^0.12.3"
  },
  "dependencies": {
    "http-server": "^0.11.1"
  }
}
